#include <iostream>

using namespace std;

int callatz(int ori, int a[n])
{
    if (ori % 2 == 0)
    {
        ori /= 2;
    }
}

int main()
{
    int n;
    cin >> n;
    int numbers[n];
    for (int i = 0; i < n; i++)
    {
        cin >> numbers[i];
    }

    for(int i = 0; i < n; i++)
    {

    }
}
